import { HighRatingBookDirective } from './high-rating-book-directive';

describe('HighRatingBookDirective', () => {
  it('should create an instance', () => {
    const directive = new HighRatingBookDirective();
    expect(directive).toBeTruthy();
  });
});
